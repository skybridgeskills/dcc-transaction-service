import type { RegistryHandler } from '../types.js'

/**
 * OIDF registry lookup (entity config JWT + federation fetch).
 * Stub until phase 5.
 */
export const lookupOidf: RegistryHandler = async () => {
  throw new Error('oidf handler: not implemented until phase 5')
}
