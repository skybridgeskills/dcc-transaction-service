import type { RegistryHandler } from '../types.js'

/**
 * VerifiableRecognitionCredential registry lookup.
 * Stub until phase 6.
 */
export const lookupVcRecognition: RegistryHandler = async () => {
  throw new Error('vc-recognition handler: not implemented until phase 6')
}
